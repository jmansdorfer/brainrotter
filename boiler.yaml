version: '3.8'

services:
  boilbot:
    build: .
    container_name: discord-boil-bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    volumes:
      - ./data/template_boiling.gif:/app/template_boiling.gif
      - ./temp:/app/temp
      - ./cache:/app/cache