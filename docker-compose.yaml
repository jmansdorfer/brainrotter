---
services:
  brainrotter:
    build: .
    container_name: brainrotter
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - DISCORD_BOT_GUILD_IDS=${DISCORD_BOT_GUILD_IDS}
    volumes:
      - ./templates:/app/templates
      - ./cache:/app/cache